# Licenses & provenance (spec §3.6 / CR-5)

Attribution for the model artifacts this site may ship. The scaffold commits
the manifest + this directory; the ONNX binaries themselves are regenerated by
`scripts/train_generator.py` (and are git-ignored, `.gitignore`).

## all-MiniLM-L6-v2 — Apache-2.0

- Upstream: sentence-transformers / Hugging Face model hub
  (`sentence-transformers/all-MiniLM-L6-v2`).
- License: **Apache-2.0** (see https://www.apache.org/licenses/LICENSE-2.0).
- Use here: on-device embedding → 384-d, converted to int8 ONNX
  (`embedder-all-minilm-l6-v2-int8.onnx`, ADR-5).
- Provenance note: this model is an imported authority (REDESIGN §5); the
  community aligner (Slice 2) and manifest-driven model swaps are the
  mitigation. English-centric support acknowledged (REQUIREMENTS §9).

## ONNX Runtime Web — MIT

- Upstream: https://github.com/microsoft/onnxruntime
- License: MIT. Used for all on-device inference (execution providers
  `webgpu`/`wasm`, int8).

## three.js — MIT

- Upstream: https://github.com/mrdoob/three.js
- License: MIT. Used for the WebGL2 render path (ACES, sRGB, PCFSoft shadows).

## The generator / decoder / sensory-v0 weights (trained in-repo)

- Generated by `scripts/train_generator.py` on the 8-seed cold-start corpus.
- License for the trained weights: this project's (see repo README) — they are
  produced from arbitrary public sentences used as unlabelled seed *samples*,
  not from any private text.

## Provenance of the dataset (DR-4)

`models.json` carries `trainingSource: { seedForms, generatedAt }` once the
training script has run, so every release's drift-provenance is traceable.
Rollback-by-pointer = swap the manifest JSON (spec §3.6).